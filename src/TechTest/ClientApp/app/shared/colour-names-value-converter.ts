import { IColour } from '../people/interfaces/icolour';

export class ColourNamesValueConverter {

  toView(colours: IColour[]) {


      var Colour = string.Join(",", Colour.ToArray());
   

    return 'Todo';
  }

}
